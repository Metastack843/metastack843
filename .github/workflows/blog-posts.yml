name: Latest Blog Posts

on:
  schedule:
    - cron: "0 12 * * *" # Runs daily at noon UTC
  workflow_dispatch: # Allows manual trigger

jobs:
  update-readme-with-blog:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Pull in latest blog posts
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          # Replace with your actual blog RSS feed URL
          feed_list: "https://dev.to/feed/metastack843,https://medium.com/feed/@metastack843"
          max_post_count: 5
          template: "- $newline🔥 [$title]($url) — *$date*"
          date_format: "MMM DD, YYYY"
          comment_tag_name: "BLOG-POST-LIST"
          commit_message: "docs: auto-update blog posts"
